class circle {
  constructor(radius) {
    this.radius = radius;
  }
  
  set diameter() {
    this.radius = diameter * 2;
  }
  
  get circumference(circumference) {
    this.radius = circumference / (2 * Math.PI);
  }
  
  get diameter
  
  
}